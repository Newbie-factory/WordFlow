# WordFlow 原批准计划整合审计（2026-08-22）

## 结论

当前版本不能诚实地标记为“原批准计划全部完成且绝对无 Bug”。可确认的是：原计划第 1–12 项的核心实现已经整合；第 13 项控制中心已恢复主要页面和真实数据链路，但仍有若干原验收项未完成。原计划第 14–16 项不属于“前 13 项”，也仍有部分或全部缺口。

旧的 `.superpowers/sdd/progress.md` 在第 13 项之后发生了编号漂移：它把 PNG 主题记作 Task 13、把 EXE 发布记作 Task 14，不能作为原批准实施计划第 13–16 项已经完成的证据。本审计以 `docs/superpowers/plans/2026-08-13-wordflow-windows-implementation.md` 为准。

## 原计划第 1–13 项

| 原计划任务 | 当前状态 | 可执行证据 / 仍缺内容 |
|---|---|---|
| 1. 工具链与解决方案骨架 | 已整合 | Release 解决方案可编译；对应任务审查记录存在。 |
| 2. 质量门禁 IELTS 词库 | 已整合 | 本地校验通过：12,046 个大小写不重复词条，必需词 46/46，SQLite integrity `ok`。 |
| 3. 词汇关系数据库 | 已整合 | 本地校验通过：43,947 senses、47,934 published relations、19,520 candidates。 |
| 4. FSRS-6 调度器 | 已整合 | 纯领域调度测试已纳入全量测试。 |
| 5. 本机 FSRS 参数优化 | 已整合 | 安全优化与回退逻辑测试已纳入全量测试。 |
| 6. 学习、斩词、取消斩和每日队列 | 已整合 | 领域/应用层支持斩词、恢复、补偿式撤销与每日队列。 |
| 7. SQLite 事件与快照 | 已整合 | 版本化、本地持久化和不可变事件测试已纳入全量测试。 |
| 8. 应用用例与界面无关状态 | 已整合 | 学习、关系词、分页用例已接入桌面端。 |
| 9. 单实例离线 WPF 应用 | 已整合 | 正常启动、单实例、托盘与跨进程恢复已有自动测试与启动冒烟。 |
| 10. 可配置快捷键 | 已整合 | F1/F2/F3 及关系词等快捷键均进入统一设置模型；冲突保护已有测试。 |
| 11. 置顶单词卡与关系抽屉 | 已整合 | 单词卡、近义词/形近词抽屉、拖动/定位与 UI 可访问性测试存在。 |
| 12. 完全离线发音 | 已整合 | Windows SAPI、本机声音检测、自动朗读与设置持久化已有测试。 |
| 13. 控制中心与 100 条已斩页 | **部分整合** | 已有六个主页面、六个设置子页、真实进度/词库/易混词/已斩/统计数据、100 条分页和单词恢复；但原验收中的 50/100/200 持久化切换、多选/全筛选选择、批量恢复确认与短撤销、个人关系增删覆盖、保持率普通设置与参数替换快照尚未完整实现。页面目前集中在一个窗口 XAML，而非原计划的独立页面 ViewModel 结构。 |

## 第 13 项之后的已知缺口

- Task 14（完整分界面皮肤）：已有 PNG 导入、持久化、透明度、恢复默认与卡片/抽屉背景；尚缺全局/分界面继承、焦点位置、模糊、保护遮罩、缓存和高对比模式等全部验收项。
- Task 15（备份/恢复/导出/诊断）：设置页目前只有说明文字，手动备份、原子恢复、JSON/CSV 导出、14 天滚动备份和恢复界面尚未实现。
- Task 16（安装与兼容矩阵）：已有自包含 Windows x64 EXE 文件夹；尚无 MSI、自动发布门禁脚本、Win10/Win11 干净虚拟机与多 DPI/多显示器证据矩阵。

## 本轮窗口外壳验证

- 控制中心使用 `WindowStyle=None`、透明窗口和单个根圆角卡片，不再显示 Windows 外框加内部卡片的双层框。
- 标题区集成最小化、最大化/还原、隐藏三个按钮；自动化验证三种状态变化均成功，隐藏后进程保持存活。
- 自动化依次进入六个主页面与六个设置子页，进程未退出。
- 真实鼠标拖动标题区后窗口坐标发生变化，证明拖动链路生效。
- 真实鼠标拖动可见圆角边缘后，窗口由 1680×1080 调整为 1770×1140，证明缩放链路生效。
- 新增窗口结构、可访问性与八方向缩放命中回归测试。

## 本轮新鲜验证证据

- `dotnet build WordFlow.sln -c Release --no-restore`：0 warnings，0 errors。
- `dotnet test WordFlow.sln -c Release --no-restore`：498/498 通过（Domain 129、Application 47、Infrastructure 200、App 122）。
- 完整词库/关系库校验：通过；两份 SQLite integrity 均为 `ok`。
- 自包含 EXE：`D:\baicizhan\release\WordFlow-win-x64\WordFlow.App.exe`；SHA-256 `1D5B16ED07FDE65881807FC5B6AD1856BE6AE0D64F0B30C37944D0BC0DDF5B87`。
- 发布版普通启动保持存活；第二次启动退出且系统中仅保留一个 WordFlow 进程，单实例链路通过。
- 视觉截图：`.superpowers/sdd/control-center-single-card.png`。

## “无 Bug”表述边界

自动测试、构建、数据完整性和当前机器上的 UI 冒烟只能证明已覆盖路径通过，不能数学上证明软件不存在任何 Bug。只有在剩余验收项完成，并补齐 Windows 10/11、DPI、显示器、损坏数据和安装/卸载矩阵后，才能把版本标为完整发布候选。
