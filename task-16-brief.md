# Task 16 brief: complete offline control center

- Persist the daily new/review target and expose the effective `DailyPlan` used by the learning queue.
- Expose real today statistics from the learning store (review events and slashed cards), never placeholder copy.
- Put shortcut editing, pronunciation, theme, data/offline/version information in one control-center window.
- Keep the floating card limited to its opacity slider.

Acceptance tests cover daily-plan persistence, queue consumption after a plan change, real statistics, and the control-center composition.
